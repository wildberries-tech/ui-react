import { Canvas, Meta, Story, StoryTable, Heading, Description } from '@storybook/addon-docs';

import { Splitter } from '../../../components-stories/splitter';
import { Checkbox } from '../../checkbox/index';
import { CheckboxGroup } from './../';

<Meta
    title="Components/Checkboxes/Checkbox Group"
    component={CheckboxGroup}
    subcomponents={{
        Checkbox
    }}
    args={{
        legend  : 'My checkbox family',
        children: [
            <Checkbox key="checkbox-1" name="checkbox-1" />,
            <Checkbox key="checkbox-2" name="checkbox-2" label="My checkbox 2" />,
            <Checkbox key="checkbox-3" name="checkbox-3" label="My checkbox 3" description="Description checkbox 3" />
        ]
    }}
    argTypes={{
        className: {
            control: 'text'
        },
        legend: {
            control: 'text'
        },
        children: {
            control: false
        }
    }}
    parameters={{
        cssProps: {
            values : {
                '--checkbox-group-gap': {
                    selector    : '#checkbox-group-css-api',
                    value       : '8px 32px',
                    description : 'Расстояние между дочерними элементами'
                },
                '--checkbox-group-legend-color': {
                    defaultValue: 'var(--color-text-headers)',
                    selector    : '#checkbox-group-css-api',
                    value       : '#4e4e53',
                    description : 'Цвет текста легенды'
                },
                '--checkbox-group-legend-font-size': {
                    defaultValue: '16px',
                    selector    : '#checkbox-group-css-api',
                    value       : '16px',
                    description : 'Размер текста легенды'
                },
                '--checkbox-group-legend-gap': {
                    defaultValue: '16px',
                    selector    : '#checkbox-group-css-api',
                    value       : '16px',
                    description : 'Расстояние между легендой и дочерними элементами'
                }
            }
        },
        design: {
            type: 'figma',
            url : 'https://www.figma.com/file/c2MtpBedt0dYAz20eepwzX/%E2%9D%96-WB-Partners-DS?node-id=6633%3A127623&t=bJms1tK1Y27PyXbH-0'
        }
    }}
/>

export const Template = (args) => <CheckboxGroup {...args } />

<Story
    name="API"
    parameters={{
        actions: {
            argTypesRegex: '^on.*'
        }
    }}
>
    {(props) => (
        <Splitter
            titles={[
                'Controls',
                'CSS Controls'
            ]}
        >
            <CheckboxGroup {...props} />
            <CheckboxGroup {...props} id="checkbox-group-css-api" />
        </Splitter>
    )}
</Story>

<Heading>Checkbox Group</Heading>
<Description>Компонент для организации визуальных групп чекбоксов.</Description>

<Heading>Использование</Heading>
<Description>Импорт компонента в код.</Description>

```tsx
import { CheckboxGroup } from '@wb-tech/ui-react';
```

<Heading>Примеры</Heading>
<Description>Ниже приведены примеры состояний описанных в дизайн системе.</Description>

<Canvas>
    <Story
        name="row-hidden-story"
        children={Template.bind({})}
        args={{
            direction: 'row'
        }}
    />
</Canvas>

<Canvas>
    <Story
        name="column-hidden-story"
        children={Template.bind({})}
        args={{
            direction: 'column'
        }}
    />
</Canvas>

<Heading>API</Heading>
<Description>Эта таблица параметров позволяет взаимодействовать с компонентом.</Description>

<Canvas withSource="open">
    <Story
        name="interactive-hidden-story"
        children={Template.bind({})}
    />
</Canvas>

<StoryTable story="interactive-hidden-story" />
