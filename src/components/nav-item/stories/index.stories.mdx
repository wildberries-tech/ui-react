import { Canvas, Meta, Story, StoryTable, Heading, Description } from '@storybook/addon-docs';

import { Splitter } from '../../../components-stories/splitter';
import { IconVideo } from './../../icons/video';
import { IconCircleMenu } from './../../icons/circle-menu';
import { NavItem } from './../';

<Meta
    title="Components/Nav/Item"
    component={NavItem}
    args={{
        children: 'Кнопка',
        href: '#'
    }}
    argTypes={{
        className: {
            control: 'text'
        },
        children: {
            control: 'text'
        },
        onClick: {
            control: false,
            table  : {
                category: 'Events'
            }
        }
    }}
    parameters={{
        cssProps: {
            values : {
                '--local-color-text': {
                    defaultValue: 'var(--color-text-basic)',
                    selector    : '#nav-item-css-api',
                    value       : '#000000',
                    description : 'Цвет текста'
                },
                '--local-color-text-disabled': {
                    defaultValue: 'var(--color-text-disabled)',
                    selector    : '#nav-item-css-api',
                    value       : '#b8b6bf',
                    description : 'Цвет текста в состояние disabled'
                },
                '--local-color-background': {
                    defaultValue: 'var(--color-bg-base)',
                    selector    : '#nav-item-css-api',
                    value       : '#ffffff',
                    description : 'Цвет фона'
                },
                '--local-color-background-hover': {
                    defaultValue: 'var(--color-bg-plate)',
                    selector    : '#nav-item-css-api',
                    value       : '#f0f0f3',
                    description : 'Цвет фона в состояние hover'
                },
                '--local-color-background-active': {
                    defaultValue: 'var(--color-bg-plate-1)',
                    selector    : '#nav-item-css-api',
                    value       : '#d1cfd7',
                    description : 'Цвет фона в состояние active (pressed)'
                },
                '--local-color-background-disabled': {
                    defaultValue: 'var(--color-bg-base)',
                    selector    : '#nav-item-css-api',
                    value       : '#ffffff',
                    description : 'Цвет фона в состояние disabled'
                },
                '--local-color-focus': {
                    defaultValue: 'var(--color-border-focused)',
                    selector    : '#nav-item-css-api',
                    value       : '#5067de',
                    description : 'Цвет рамки фокуса'
                }
            }
        },
        design: {
            type: 'figma',
            url : 'https://www.figma.com/file/c2MtpBedt0dYAz20eepwzX/%E2%9D%96-WB-Partners-DS?node-id=3250%3A113145'
        }
    }}
/>

export const Template = (args) => <NavItem {...args } />

<Story
    name="API"
    parameters={{
        actions: {
            argTypesRegex: '^on.*'
        }
    }}
>
    {(props) => (
        <Splitter
            titles={[
                'Controls',
                'CSS Controls'
            ]}
        >
            <NavItem {...props} />
            <NavItem {...props} id="nav-item-css-api" />
        </Splitter>
    )}
</Story>

<Heading>NavItem</Heading>
<Description>TODO</Description>

<Heading>Использование</Heading>
<Description>Импорт компонента в код.</Description>

```tsx
import { NavItem } from '@wb-tech/ui-react';
```

<Heading>Примеры</Heading>
<Description>Ниже приведены примеры состояний описанных в дизайн системе.</Description>

<Canvas>
    <Story
        name="default-hidden-story"
        children={Template.bind({})}
    />
    <Story
        name="active-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'active'
        }}
    />
</Canvas>

<Heading>API</Heading>
<Description>Эта таблица параметров позволяет взаимодействовать с компонентом.</Description>

<Canvas withSource="open">
    <Story
        name="interactive-hidden-story"
        children={Template.bind({})}
    />
</Canvas>

<StoryTable story="interactive-hidden-story" />
