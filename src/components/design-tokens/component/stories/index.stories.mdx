import { Canvas, Meta, Story, StoryTable, Heading, Description } from '@storybook/addon-docs';

import Source from '../../hook/stories/source.docs.mdx';

import { DesignTokens } from './../';

<Meta
    title="Components/Design Tokens/Component"
    component={DesignTokens}
    args={{
        children: 'Любое содержимое из типа ReactNode'
    }}
    argTypes={{
        children: {
            control: 'text'
        },
        className: {
            control: 'text'
        }
    }}
    parameters={{
        design: {
            type: 'figma',
            url : 'https://www.figma.com/file/c2MtpBedt0dYAz20eepwzX/%E2%9D%96-WB-Partners-DS?node-id=5%3A17'
        }
    }}
/>

export const Template = (args) => <DesignTokens {...args } />

<Story
    name="API"
    parameters={{
        actions: {
            argTypesRegex: '^on.*'
        }
    }}
    children={Template.bind({})}
/>

<Heading>Design Tokens</Heading>
<Description>Предоставляет набор дизайн токенов в виде CSS переменных. Все предоставляемые переменные ограничены областью использования компонента или хука.</Description>
<Description>Содержит параметры: цветовая палитра, типографика, скругления, тени.</Description>

<Heading>Использование</Heading>
<Description>Импорт компонента в код.</Description>

```tsx
import { DesignTokens } from '@wb-tech/ui-react';
```

<Description>Передайте в компонент область в которой необходимо получить доступ к переменным. Ниже приведен наиболее частый случай применения где областью является все приложение.</Description>

```tsx
import React from 'react';
import { render } from 'react-dom';

import { DesignTokens } from '@wb-tech/ui-react';

render((
    <DesignTokens>
        <YourAppCode />
    </DesignTokens>
), document.getElementById('root'));
```

<Heading>API</Heading>
<Description>Эта таблица параметров позволяет взаимодействовать с компонентом.</Description>

<Canvas withSource="open">
    <Story
        name="interactive-hidden-story"
        children={Template.bind({})}
    />
</Canvas>

<StoryTable story="interactive-hidden-story" />

<Source />
