import { Canvas, Meta, Story, StoryTable, Heading, Description } from '@storybook/addon-docs';

import { Splitter } from '../../../components-stories/splitter';
import { IconVideo } from './../../icons/video';
import { IconCircleMenu } from './../../icons/circle-menu';
import { Button } from './../';

<Meta
    title="Components/Buttons/Button"
    component={Button}
    args={{
        children: 'Кнопка'
    }}
    argTypes={{
        className: {
            control: 'text'
        },
        children: {
            control: 'text'
        },
        onFocus: {
            control: false,
            table  : {
                category: 'Events'
            }
        },
        onClick: {
            control: false,
            table  : {
                category: 'Events'
            }
        }
    }}
    parameters={{
        cssProps: {
            values : {
                '--button-font-size': {
                    defaultValue: 'undefined',
                    selector    : '#button-css-api',
                    value       : '16px',
                    description : 'Размер текста'
                }
            }
        },
        design: {
            type: 'figma',
            url : 'https://www.figma.com/file/c2MtpBedt0dYAz20eepwzX/%E2%9D%96-WB-Partners-DS?node-id=3250%3A113145'
        }
    }}
/>

export const Template = (args) => <Button {...args } />

<Story
    name="API"
    parameters={{
        actions: {
            argTypesRegex: '^on.*'
        }
    }}
>
    {(props) => (
        <Splitter
            titles={[
                'Controls',
                'CSS Controls'
            ]}
        >
            <Button {...props} />
            <Button {...props} id="button-css-api" />
        </Splitter>
    )}
</Story>

<Heading>Button</Heading>
<Description>Компонент конфигурируемой кнопки соответствующий дизайну.</Description>

<Heading>Использование</Heading>
<Description>Импорт компонента в код.</Description>

```tsx
import { Button } from '@wb-tech/ui-react';
```

<Heading>Примеры</Heading>
<Description>Ниже приведены примеры состояний описанных в дизайн системе.</Description>

<Canvas>
    <Story
        name="primary-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'primary'
        }}
    />
    <Story
        name="default-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'default'
        }}
    />
    <Story
        name="dashed-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'dashed'
        }}
    />
    <Story
        name="ghost-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'ghost'
        }}
    />
    <Story
        name="negative-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'negative'
        }}
    />
    <Story
        name="success-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'success'
        }}
    />
    <Story
        name="deprecated-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'deprecated'
        }}
    />
</Canvas>

<Canvas>
    <Story
        name="icon-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'primary',
            children: (
                <IconVideo />
            )
        }}
    />
    <Story
        name="icon-double-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'primary',
            children   : [
                <IconVideo key={1} />,
                <IconCircleMenu key={2} />
            ]
        }}
    />
</Canvas>

<Canvas>
    <Story
        name="icon-text-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'default',
            children   : [
                <IconVideo key={1} />,
                'Кнопка'
            ]
        }}
    />
    <Story
        name="icon-text-icon-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'default',
            children   : [
                <IconVideo key={1} />,
                'Кнопка',
                <IconCircleMenu key={2} />
            ]
        }}
    />
</Canvas>

<Heading>API</Heading>
<Description>Эта таблица параметров позволяет взаимодействовать с компонентом.</Description>

<Canvas withSource="open">
    <Story
        name="interactive-hidden-story"
        children={Template.bind({})}
    />
</Canvas>

<StoryTable story="interactive-hidden-story" />
