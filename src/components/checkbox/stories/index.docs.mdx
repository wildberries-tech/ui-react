import { Canvas, Story, ArgsTable } from '@storybook/addon-docs';

import { Checkbox } from './../';

## Checkbox
Компонент `checkbox` соответствующий дизайну и его состояния.
Имеет максимально нативный API для беспроблемной работы c библиотеками управления формами такие как `react-hook-form`, `formik`, `final-form` и т.д.

## Использование
Импорт компонента в код.
```tsx
import { Checkbox } from '@wb-tech/ui-react';
```

Ниже приведены примеры всех состояний описанных в дизайн системе.
<Canvas>
    <Story id="components-checkbox--no-text" />
</Canvas>

<Canvas>
    <Story id="components-checkbox--text" />
</Canvas>

<Canvas>
    <Story id="components-checkbox--text-and-description" />
</Canvas>

## Параметры
<ArgsTable of={Checkbox} />
