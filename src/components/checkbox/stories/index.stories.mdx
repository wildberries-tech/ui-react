import { Canvas, Meta, Story, StoryTable, Heading, Description } from '@storybook/addon-docs';

import { Splitter } from '../../../components-stories/splitter';
import { Checkbox } from './../';

<Meta
    title="Components/Checkboxes/Checkbox"
    component={Checkbox}
    args={{
        name: 'my-checkbox'
    }}
    argTypes={{
        className: {
            control: 'text'
        },
        label: {
            control: 'text'
        },
        description: {
            control: 'text'
        },
        onChange: {
            control: false,
            table  : {
                category: 'Events'
            }
        },
        onBlur: {
            control: false,
            table  : {
                category: 'Events'
            }
        },
        onFocus: {
            control: false,
            table  : {
                category: 'Events'
            }
        },
        onClick: {
            control: false,
            table  : {
                category: 'Events'
            }
        }
    }}
    parameters={{
        cssProps: {
            values : {
                '--checkbox-label-color': {
                    defaultValue: 'var(--color-text-basic)',
                    selector    : '#checkbox-css-api',
                    value       : '#000000',
                    description : 'Цвет текста лейбла'
                }
            }
        },
        design: {
            type: 'figma',
            url : 'https://www.figma.com/file/c2MtpBedt0dYAz20eepwzX/%E2%9D%96-WB-Partners-DS?node-id=3273%3A113867'
        }
    }}
/>

export const Template = (args) => <Checkbox {...args } />

<Story
    name="API"
    parameters={{
        actions: {
            argTypesRegex: '^on.*'
        }
    }}
>
    {(props) => (
        <Splitter
            titles={[
                'Controls',
                'CSS Controls'
            ]}
        >
            <Checkbox {...props} />
            <Checkbox {...props} id="checkbox-css-api" />
        </Splitter>
    )}
</Story>

<Heading>Checkbox</Heading>
<Description markdown="Компонент `checkbox` соответствующий дизайну и его состояния." />
<Description markdown="Имеет максимально нативный API для беспроблемной работы c библиотеками управления формами такие как `react-hook-form`, `formik`, `final-form` и т.д." />

<Heading>Использование</Heading>
<Description>Импорт компонента в код.</Description>

```tsx
import { Checkbox } from '@wb-tech/ui-react';
```

<Heading>Примеры</Heading>
<Description>Ниже приведены примеры состояний описанных в дизайн системе.</Description>

<Canvas>
    <Story
        name="no-text-hidden-story"
        children={Template.bind({})}
    />
</Canvas>

<Canvas>
    <Story
        name="text-hidden-story"
        children={Template.bind({})}
        args={{
            label: 'Checkbox label'
        }}
    />
</Canvas>

<Canvas>
    <Story
        name="text-and-description-hidden-story"
        children={Template.bind({})}
        args={{
            label      : 'Checkbox label',
            description: 'Description checkbox'
        }}
    />
</Canvas>

<Heading>API</Heading>
<Description>Эта таблица параметров позволяет взаимодействовать с компонентом.</Description>

<Canvas withSource="open">
    <Story
        name="interactive-hidden-story"
        children={Template.bind({})}
    />
</Canvas>

<StoryTable story="interactive-hidden-story" />
