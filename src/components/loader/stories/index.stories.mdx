import { Canvas, Meta, Story, StoryTable, Heading, Description } from '@storybook/addon-docs';

import { Splitter } from '../../../components-stories/splitter';
import { Loader } from './../';

<Meta
    title="Components/Loader"
    component={Loader}
    argTypes={{
        className: {
            control: 'text'
        }
    }}
    parameters={{
        cssProps: {
            values : {
                '--loader-size-custom': {
                    defaultValue: 'undefined',
                    selector    : '#loader-css-api',
                    value       : '32px',
                    description : 'Размер лоадера'
                },
                '--loader-color-custom': {
                    defaultValue: 'undefined',
                    selector    : '#loader-css-api',
                    value       : '#0ef14e',
                    description : 'Цвет лоадера'
                },
                '--loader-animation-speed': {
                    selector    : '#loader-css-api',
                    value       : '2s',
                    description : 'Скорость вращения'
                }
            }
        },
        design: {
            type: 'figma',
            url : 'https://www.figma.com/file/c2MtpBedt0dYAz20eepwzX/%E2%9D%96-WB-Partners-DS?node-id=5%3A15787'
        }
    }}
/>

export const Template = (args) => <Loader {...args } />

<Story
    name="API"
    args={{
        presetStyle: 'rich-grey'
    }}
>
    {(props) => (
        <Splitter
            titles={[
                'Controls',
                'CSS Controls'
            ]}
        >
            <Loader {...props} />
            <Loader {...props} id="loader-css-api" />
        </Splitter>
    )}
</Story>

<Heading>Loader</Heading>
<Description>Компонент процесса загрузки соответствующий дизайну и его состояния.</Description>

<Heading>Использование</Heading>
<Description>Импорт компонента в код.</Description>

```tsx
import { Loader } from '@wb-tech/ui-react';
```

<Heading>Примеры</Heading>
<Description>Ниже приведены примеры состояний описанных в дизайн системе.</Description>

<Canvas className="hack-dark-theme">
    <Story
        name="default-small-hidden-story"
        children={Template.bind({})}
        args={{
            presetSize: 'small'
        }}
    />
    <Story
        name="default-medium-hidden-story"
        args={{
            presetSize: 'medium'
        }}
        children={Template.bind({})}
    />
    <Story
        name="default-large-hidden-story"
        args={{
            presetSize: 'large'
        }}
        children={Template.bind({})}
    />
</Canvas>

<Canvas>
    <Story
        name="rich-grey-small-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'rich-grey',
            presetSize : 'small'
        }}
    />
    <Story
        name="rich-grey-medium-hidden-story"
        args={{
            presetStyle: 'rich-grey',
            presetSize : 'medium'
        }}
        children={Template.bind({})}
    />
    <Story
        name="rich-grey-large-hidden-story"
        args={{
            presetStyle: 'rich-grey',
            presetSize : 'large'
        }}
        children={Template.bind({})}
    />
</Canvas>

<Canvas>
    <Story
        name="grey-small-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'grey',
            presetSize : 'small'
        }}
    />
    <Story
        name="grey-medium-hidden-story"
        args={{
            presetStyle: 'grey',
            presetSize : 'medium'
        }}
        children={Template.bind({})}
    />
    <Story
        name="grey-large-hidden-story"
        args={{
            presetStyle: 'grey',
            presetSize : 'large'
        }}
        children={Template.bind({})}
    />
</Canvas>

<Canvas>
    <Story
        name="red-small-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'red',
            presetSize : 'small'
        }}
    />
    <Story
        name="red-medium-hidden-story"
        args={{
            presetStyle: 'red',
            presetSize : 'medium'
        }}
        children={Template.bind({})}
    />
    <Story
        name="red-large-hidden-story"
        args={{
            presetStyle: 'red',
            presetSize : 'large'
        }}
        children={Template.bind({})}
    />
</Canvas>

<Canvas>
    <Story
        name="light-green-small-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'light-green',
            presetSize : 'small'
        }}
    />
    <Story
        name="light-green-medium-hidden-story"
        args={{
            presetStyle: 'light-green',
            presetSize : 'medium'
        }}
        children={Template.bind({})}
    />
    <Story
        name="light-green-large-hidden-story"
        args={{
            presetStyle: 'light-green',
            presetSize : 'large'
        }}
        children={Template.bind({})}
    />
</Canvas>

<Canvas>
    <Story
        name="dark-purple-small-hidden-story"
        children={Template.bind({})}
        args={{
            presetStyle: 'dark-purple',
            presetSize : 'small'
        }}
    />
    <Story
        name="dark-purple-medium-hidden-story"
        args={{
            presetStyle: 'dark-purple',
            presetSize : 'medium'
        }}
        children={Template.bind({})}
    />
    <Story
        name="dark-purple-large-hidden-story"
        args={{
            presetStyle: 'dark-purple',
            presetSize : 'large'
        }}
        children={Template.bind({})}
    />
</Canvas>

<Heading>API</Heading>
<Description>Эта таблица параметров позволяет взаимодействовать с компонентом.</Description>

<Canvas withSource="open">
    <Story
        name="interactive-hidden-story"
        children={Template.bind({})}
    />
</Canvas>

<StoryTable story="interactive-hidden-story" />
