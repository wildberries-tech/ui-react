import { Canvas, Meta, Story, StoryTable, Heading, Description } from '@storybook/addon-docs';

import { IconGlobe } from './../../icons/globe';
import { IconVerified } from './../../icons/verified';
import { Splitter } from '../../../components-stories/splitter';
import { Tag } from './../';

<Meta
    title="Components/Tag"
    component={Tag}
    args={{
        children: 'Название тега'
    }}
    argTypes={{
        className: {
            control: 'text'
        },
        children: {
            control: 'text'
        },
        onClick: {
            control: false,
            table  : {
                category: 'Events'
            }
        },
        onDrag: {
            control: false,
            table  : {
                category: 'Events'
            }
        },
        onMouseEnter: {
            control: false,
            table  : {
                category: 'Events'
            }
        },
        onMouseLeave: {
            control: false,
            table  : {
                category: 'Events'
            }
        }
    }}
    parameters={{
        cssProps: {
            values : {
                '--tag-gap': {
                    value      : '8px',
                    selector   : '#tag-css-api',
                    description: 'Расстояние между дочерними элементами'
                },
                '--tag-padding': {
                    value      : '4px 8px',
                    selector   : '#tag-css-api',
                    description: 'Расстояние до границ'
                },
                '--tag-font-size': {
                    value      : '13px',
                    selector   : '#tag-css-api',
                    description: 'Размер текста'
                },
                '--tag-font-weight': {
                    value      : '400',
                    selector   : '#tag-css-api',
                    description: 'Толщина текста'
                },
                '--tag-line-height': {
                    value      : '16px',
                    selector   : '#tag-css-api',
                    description: 'Высота строки'
                },
                '--tag-color': {
                    defaultValue: 'var(--color-text-basic)',
                    selector    : '#tag-css-api',
                    value       : '#000',
                    description : 'Цвет текста и SVG элементов'
                },
                '--tag-bg-color': {
                    defaultValue: 'var(--color-bg-plate)',
                    selector    : '#tag-css-api',
                    value       : '#F0F0F3',
                    description : 'Фоновый цвет'
                }
            }
        },
        design: {
            type: 'figma',
            url : 'https://www.figma.com/file/c2MtpBedt0dYAz20eepwzX/%E2%9D%96-WB-Partners-DS?node-id=3062%3A107510&t=FhPcR2TbHtglIp7i-0'
        }
    }}
/>

export const Template = (args) => <Tag {...args } />

<Story
    name="API"
    parameters={{
        actions: {
            argTypesRegex: '^on.*'
        }
    }}
>
    {(props) => (
        <Splitter
            titles={[
                'Controls',
                'CSS Controls'
            ]}
        >
            <Tag {...props} />
            <Tag {...props} id="tag-css-api" />
        </Splitter>
    )}
</Story>

<Heading>Tag (Badge)</Heading>
<Description>Компонент отображени бейджа или тега.</Description>

<Heading>Использование</Heading>
<Description>Импорт компонента в код.</Description>

```tsx
import { Tag } from '@wb-tech/ui-react';
```

<Heading>Примеры</Heading>
<Description>Ниже приведены примеры состояний описанных в дизайн системе.</Description>

<Canvas>
    <Story
        name="Badge (success)-hidden-story"
        args={{
            presetStyle: 'badge-success'
        }}
        children={Template.bind({})}
    />
    <Story
        name="Badge (processing)-hidden-story"
        args={{
            presetStyle: 'badge-processing'
        }}
        children={Template.bind({})}
    />
    <Story
        name="Badge (rollbacked)-hidden-story"
        args={{
            presetStyle: 'badge-rollbacked'
        }}
        children={Template.bind({})}
    />
    <Story
        name="Badge (active)-hidden-story"
        args={{
            presetStyle: 'badge-active'
        }}
        children={Template.bind({})}
    />
    <Story
        name="Badge (failed)-hidden-story"
        args={{
            presetStyle: 'badge-failed'
        }}
        children={Template.bind({})}
    />
    <Story
        name="Badge (purple)-hidden-story"
        args={{
            presetStyle: 'badge-purple'
        }}
        children={Template.bind({})}
    />
</Canvas>

<Canvas>
    <Story
        name="Tag (small)-hidden-story"
        args={{
            presetStyle: 'tag',
            presetSize : 'small'
        }}
        children={Template.bind({})}
    />
    <Story
        name="Tag (medium)-hidden-story"
        args={{
            presetStyle: 'tag',
            presetSize : 'medium'
        }}
        children={Template.bind({})}
    />
    <Story
        name="Tag (large)-hidden-story"
        args={{
            presetStyle: 'tag',
            presetSize : 'large'
        }}
        children={Template.bind({})}
    />
</Canvas>

<Canvas>
    <Story
        name="Tag (icon before)-hidden-story"
        args={{
            presetStyle: 'tag',
            presetSize : 'small',
            children   : [
                <IconGlobe key="icon-before" />,
                'Название тега'
            ]
        }}
        children={Template.bind({})}
    />
    <Story
        name="Tag (icon after)-hidden-story"
        args={{
            presetStyle: 'tag',
            presetSize : 'medium',
            children   : [
                'Название тега',
                <IconVerified key="icon-after" />
            ]
        }}
        children={Template.bind({})}
    />
    <Story
        name="Tag (icons)-hidden-story"
        args={{
            presetStyle: 'tag',
            presetSize : 'large',
            children   : [
                <IconGlobe key="icon-before" />,
                'Название тега',
                <IconVerified key="icon-after" />
            ]
        }}
        children={Template.bind({})}
    />
</Canvas>

<Heading>API</Heading>
<Description>Эта таблица параметров позволяет взаимодействовать с компонентом.</Description>

<Canvas withSource="open">
    <Story
        name="interactive-hidden-story"
        children={Template.bind({})}
    />
</Canvas>

<StoryTable story="interactive-hidden-story" />
