import { Meta } from '@storybook/addon-docs';

<Meta
    title="Builders/Webpack 5"
    parameters={{
        backgrounds: {
            disable: true,
            grid: {
                disable: true
            }
        },
        viewMode: 'docs',
        previewTabs: {
            canvas: {
                hidden: true
            }
        }
    }}
/>

## Использование с Webpack 5
Для корректной сборки кита при использование webpack 5 и выше необходимо воспользоваться расширением конфига сборки.

Конфигурация до расширения:
```js
// webpack.config.js
module.exports = {
    entry: {
        main: './src/index.js'
    },
    output: {
        path    : './dist',
        filename: '[name].bundle.js'
    },
    ...
};
```

Конфигурация после расширения:
```js
// webpack.config.js
const { extendWebpackConfig } = require('@wb-tech/ui-react/builders');

module.exports = extendWebpackConfig({
    entry: {
        main: './src/index.js'
    },
    output: {
        path    : './dist',
        filename: '[name].bundle.js'
    },
    ...
});
```
